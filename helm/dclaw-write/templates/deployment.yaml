apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dclaw-write.fullname" . }}
  labels:
    {{- include "dclaw-write.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "dclaw-write.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dclaw-write.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "dclaw-write.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8105
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
