apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-write.fullname" . }}
  labels:
    {{- include "dclaw-write.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-write.selectorLabels" . | nindent 4 }}
